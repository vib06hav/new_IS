# QA Remediation Implementation Plan

## Goal
Fix every open issue in [Docs/stage_1_7_qa_analysis.md](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/Docs/stage_1_7_qa_analysis.md) so the admin/interviewer workflow is secure, browser-correct, and regression-covered.

## Tasks
- [x] Task 1: Lock down privileged registration. Replace anonymous `POST /auth/register` in [app/auth/router.py](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/app/auth/router.py) and [app/auth/service.py](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/app/auth/service.py) with an admin-protected interviewer-creation flow, and narrow public auth behavior so admin creation is no longer user-driven. Update [frontend/lib/api.ts](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/lib/api.ts) and [frontend/app/admin/interviewers/page.tsx](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/app/admin/interviewers/page.tsx) to call the new endpoint. Verify: anonymous privileged registration returns `401` or `403`, admin interviewer creation still works, and wrong-role portal logins still fail cleanly.
- [x] Task 2: Add upload content validation and controlled failure mapping. Harden [app/api/applications.py](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/app/api/applications.py) so it validates PDF content before the deterministic pipeline runs and converts parser/layout failures into structured client-facing errors instead of raw `500`s. If needed, add a small reusable validator/helper near the upload path. Verify: fake `.pdf` upload returns a controlled `4xx`, valid sample PDFs still reach `READY`, and true pipeline failures still mark the application `FAILED` with a readable `detail`.
- [x] Task 3: Fix authenticated browser delivery of source PDFs. Replace the plain anchor in [frontend/components/ReviewPackageSection.tsx](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/components/ReviewPackageSection.tsx) with an authenticated browser flow that uses the bearer token from [frontend/lib/auth.ts](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/lib/auth.ts) and a helper in [frontend/lib/api.ts](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/lib/api.ts) to fetch/open the PDF safely. Keep [app/api/applications.py](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/app/api/applications.py) authorization rules unchanged unless the chosen transport requires a small response-header adjustment. Verify: admin and assigned interviewer can click `Open source PDF` and get the PDF, unassigned interviewer still gets `403`, and the browser no longer shows `401 Not authenticated`.
- [x] Task 4: Make interviewer deletion explicit and safe. Redesign delete behavior across [app/api/users.py](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/app/api/users.py), [frontend/lib/api.ts](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/lib/api.ts), and [frontend/app/admin/interviewers/page.tsx](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/app/admin/interviewers/page.tsx) so the admin sees impact before removal. Decide and encode one policy: either block deletion when active assignments exist, or require explicit confirmation with a clear consequence message if reset-to-`READY` remains intentional. Verify: delete on an unassigned interviewer is safe, delete on an assigned interviewer either blocks with a clear error or requires confirmation and then performs the documented behavior.
- [x] Task 5: Fix stale success and error banners. In [frontend/app/admin/interviewers/page.tsx](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/app/admin/interviewers/page.tsx) and [frontend/app/admin/upload/page.tsx](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/frontend/app/admin/upload/page.tsx), clear stale `message` and `error` state at the start of each mutation and normalize post-success and post-failure state transitions. Apply the same pattern anywhere else the admin/interviewer workflow mutates state in-place. Verify: success followed by failure shows only the latest error, failure followed by success shows only the latest success, and no stale banner survives a later action.
- [x] Task 6: Stabilize frontend dev-session behavior enough for QA. Investigate the stale-session route `500`s by checking Next dev artifact handling around `.next`, route chunks, and the local dev startup flow. If the issue is reproducible, add a documented cleanup or startup safeguard in the frontend workflow rather than leaving QA dependent on lucky restarts. Verify: after a clean restart the app remains stable through repeated admin/interviewer route navigation, and the repo contains a repeatable local recovery step if stale `.next` state is the root cause.
- [x] Task 7: Add backend regression tests for each fixed server-side issue. Extend or add tests under `tests/` for privileged registration blocking, interviewer creation permissions, malformed upload handling, guarded PDF access, and interviewer deletion policy side effects. Verify: targeted `pytest` coverage fails before the fix and passes after it, with explicit assertions for HTTP status and state transitions.
- [x] Task 8: Add frontend/browser regression coverage for each fixed UI issue. Add Playwright coverage under the frontend test setup for authenticated PDF opening, duplicate interviewer-create banner behavior, upload validation feedback, assigned-interviewer deletion handling, and role-correct login flows. Verify: the browser suite catches the current bugs before the fix and passes afterward on a clean frontend session.
- [x] Task 9: Update docs and operator guidance once behavior is final. Refresh [Docs/stage_1_7_qa_analysis.md](/c:/Users/vibha/OneDrive/Desktop/AG_InterviewStandardiser/Docs/stage_1_7_qa_analysis.md) to mark resolved findings, and add any needed notes about the final delete policy, upload validation semantics, and local QA startup/recovery steps. Verify: docs match shipped behavior and no open QA finding remains undocumented.

## Done When
- [x] Anonymous privileged registration is blocked.
- [x] Admin-created interviewer flow still works.
- [x] Fake PDF uploads no longer surface raw `500`s.
- [x] Source PDF opens correctly from both admin and interviewer detail pages.
- [x] Interviewer deletion behavior is explicit, confirmed, and tested.
- [x] Success and error banners always reflect only the latest action.
- [x] Frontend QA sessions have a repeatable stability path.
- [x] Backend and browser regression coverage exists for every previously open QA issue.

## Notes
- Use the existing guarded PDF endpoint unless there is a strong reason to change the backend contract.
- Prefer blocking destructive interviewer deletion over silently resetting linked application state unless product requirements explicitly want reassignment reset behavior.
- Keep the fixes staged in this order: security first, then browser-correct review flow, then destructive-action safety, then UX polish, then QA hardening.
- Playwright regression coverage now lives under `frontend/e2e` and is run with `npm run test:e2e`.
